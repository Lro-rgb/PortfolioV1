{
  "name": "luis-rosado-portfolio",
  "version": "1.0.0",
  "description": "Portfolio-Website von Luis Rosado, IMS Bern, Bewerbung Praktikum",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "node scripts/build.js",
    "test": "node --test test/",
    "deploy": "vercel --prod",
    "hash-password": "node scripts/generate-password-hash.js",
    "generate-secret": "node scripts/generate-jwt-secret.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
